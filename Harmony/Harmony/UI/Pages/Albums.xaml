<local:BasePage x:Class="Harmony.UI.Pages.Albums"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:Harmony.UI.Pages"
      xmlns:i="http://schemas.microsoft.com/expression/2010/interactivity"
      mc:Ignorable="d" 
      d:DesignHeight="450" d:DesignWidth="800"
      DataContext="{Binding AlbumsVM, Source={StaticResource Locator}}">

    <!-- Navigation Bar List -->
    <ListView ItemsSource="{Binding AlbumItems}"
              ItemTemplate="{StaticResource AlbumItemDataTemplate}"
              ScrollViewer.VerticalScrollBarVisibility="Auto"
              VerticalAlignment="Stretch"
              HorizontalAlignment="Stretch"
              Tag="There is no album"/>

</local:BasePage>
